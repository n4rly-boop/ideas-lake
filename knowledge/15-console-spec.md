# 15 — Консоль: открытие идеи, состояние вкладок, телефон

Лёгкая спека под три дефекта `lake/api/console.html`, найденные при работе с циферблатом
2026-08-02. Файл один, сборки нет, зависимостей нет — это ограничение остаётся.

Адреса — по состоянию репозитория на коммит `90cdbe3`.

---

## §0 Что сломано

### 0.1 Клик по идее ничего не открывает

`openIdea` объявлена заглушкой и присваивается только внутри монтирования вкладки «Идеи»:

```
console.html:1129   let openIdea = () => {};
console.html:1229   openIdea = (id) => { switchTo("ideas"); setTimeout(() => show(id), 0); };
```

Пока вкладка «Идеи» ни разу не открывалась, `openIdea` — пустая функция. Пять мест зовут
её и все пять мертвы на свежей загрузке:

| Адрес | Что не работает |
|---|---|
| `console.html:876` | клик по узлу-идее на циферблате |
| `console.html:1010` | ссылка `idea_…` в таблице попаданий циферблата |
| `console.html:1093` | ссылка `idea_…` во вкладке «Индекс» |
| `console.html:1272` | ссылка `idea_…` во вкладке «Тезисы» |
| `console.html:1803` | кнопка «Открыть в „Идеях“» во вкладке «Граф» |

Воспроизведение (проверено в браузере): открыть `/ui#dial` заново, разложить гипотезу,
кликнуть узел → `location.hash` остаётся `#dial`, `typeof openIdea === "function"` (это
заглушка), ничего не открывается. После ручного захода во вкладку «Идеи» те же клики
работают. Отсюда и ощущение «через раз».

Второй дефект того же места: `setTimeout(() => show(id), 0)` — гонка с монтированием, а не
передача аргумента. Порядок держится случайно.

### 0.2 Состояние вкладки не переживает переключение

`render()` сносит `#main` и монтирует view заново, состояния нет нигде:

```
console.html:1908   function render() {
console.html:1914     main.replaceChildren();
console.html:1917     chosen.mount(main);
```

Проверено: на циферблате запрос + 12 строк таблицы + SVG; переход `#search` и обратно на
`#dial` → поле пустое, строк 0, SVG нет. Теряется:

- циферблат: текст гипотезы, `k`, число идей, флаги «идеи и рёбра» / «точки-листья», сам ответ;
- retrieve: запрос, `k`, ответ, обе руки абляции;
- идеи / тезисы / источники: `offset` и фильтры (`state` живёт в замыкании mount, `console.html:1133`);
- ингест: развёрнутая очередь заданий и автообновление.

Переживает переключение только `GRAPH_CACHE` и `LEAF_TEXT` — они объявлены на модуле
(`console.html:591`, `:594`), а не внутри view.

### 0.3 Телефона нет

На весь файл одна медиа-строка:

```
console.html:151   @media (max-width:1100px){.ab{grid-template-columns:1fr}}
```

`<meta name="viewport">` на месте (`console.html:5`), дальше всё сделано под десктоп:

- `nav.tabs{position:sticky;top:53px}` (`console.html:48`) — 53 px это высота хедера в одну
  строку. На узком экране хедер переносится (`flex-wrap:wrap`, `console.html:35`) и занимает
  две-три строки: полоса вкладок налезает на чипы или висит с разрывом.
- `label.f.grow{flex:1 1 340px}` (`console.html:67`) — поле не сжимается ниже 340 px, ряд
  контролов ломается на телефоне шириной 390.
- Таблицы (`table.tbl`, `console.html:93`) по 7 колонок в `.scroller{max-height:60vh}`
  (`console.html:101`): горизонтальный скролл внутри вертикального, читать нечем.
- Циферблат считает размер от ширины обёртки и высоты окна
  (`console.html:736`): на 390 px радиус выходит ~150 px, 3 040 точек в такой круг — каша,
  а узлы идей 7–11 px перекрывают друг друга.
- На 390 px страница **уезжает вбок**: содержимое шире вьюпорта, чипы и текст обрезаны
  правым краем (снято headless Chrome, `scratchpad/phone390.png`). Шапка с чипами занимает
  ~470 px — 56 % экрана 844 px — до первого контрола.
- **Наведения на телефоне не существует.** Карточка привязана к `mousemove` и
  `mouseleave` (`console.html:942`, `:988`). Тач их не шлёт — после переноса текста в
  ховер (коммит `90cdbe3`) на телефоне текста не видно вообще нигде, кроме таблицы под
  циферблатом. Это регрессия ровно для того сценария, который назвал владелец: «тыкать
  будут с телефона».

---

## §1 Что должно стать правдой

1. Клик по идее открывает идею — с первого раза, из любой вкладки, без предварительного
   визита в «Идеи».
2. Ссылка на состояние — это URL. Открытая вкладка, запрос гипотезы и параметры выдачи
   восстанавливаются из адреса; переключение вкладок туда-обратно ничего не теряет.
3. Консоль работает пальцем на экране 390×844: контролы в один столбец, таблицы читаемы,
   текст метки достаётся тапом, циферблат остаётся честным (числа те же, что на десктопе).
4. Ни одна из правок не добавляет зависимость, сборку и второй файл.

---

## §2 Решения

### 2.1 Маршрут вместо заглушки

Хеш становится двусоставным: `#<tab>` или `#<tab>/<arg>`.

```
#dial                     вкладка
#ideas/idea_8465d6783476  вкладка + аргумент
```

- `parseHash(hash) -> {tab, arg}` и `formatHash(tab, arg)` — две чистые функции рядом с
  роутером (`console.html:1900`).
- Сигнатура view меняется: `mount(host, ctx)`, где `ctx = {arg, state}`. Вкладка «Идеи»
  на монтировании сама зовёт `show(ctx.arg)`, если аргумент есть.
- `openIdea(id)` становится одной строкой: `location.hash = formatHash("ideas", id)`.
  Заглушка `console.html:1129` и `setTimeout(…, 0)` (`console.html:1229`) удаляются.
- Побочный выигрыш: ссылка на конкретную идею становится ссылкой, которую можно кинуть
  в чат.

Аргумент — только идентификатор идеи (`idea_…`). Ничего длиннее в путь хеша не кладём:
запрос гипотезы едет в query-часть, см. 2.2.

### 2.2 Состояние: параметры — в URL, ответы — в памяти

Правило деления:

| Что | Где живёт | Почему |
|---|---|---|
| вкладка, аргумент | хеш | это адрес |
| текст гипотезы, `k`, число идей, флаги, `offset`, фильтры | query внутри хеша: `#dial?q=…&k=12&top=30&leaves=1` | параметры воспроизводят вид и передаются ссылкой |
| тела ответов (`/dial`, `/retrieve`, страницы) | `VIEW_STATE` — `Map` на модуле, рядом с `GRAPH_CACHE` (`console.html:591`) | 285 КБ на вызов; в `sessionStorage` это данные озера в хранилище браузера, а туда мы ничего из озера не кладём (README, раздел про `/ui`) |

Механика:

- `const VIEW_STATE = new Map()` — ключ `tab`, значение — объект вкладки. `render()`
  передаёт его в `mount` и никогда не чистит.
- View на монтировании: если в состоянии лежит прошлый ответ — **рисует его**, не повторяя
  запрос. Кнопка «Разложить» / «Спросить» остаётся единственным способом сходить в сеть.
- Из URL восстанавливаются только параметры. **Автозапроса при открытии нет** — на
  телефоне это чужой трафик и лишний вызов; поле заполнено, кнопка подсвечена. Исключение
  одно: явный `&run=1` в ссылке, чтобы «покажи вот это» открывалось само.
- `switchTo` и обработчик `hashchange` (`console.html:1902`, `:1920`) переписываются под
  `parseHash`; текущая склейка `location.hash.slice(1)` уходит.

Что НЕ сохраняем: ключ (`sessionStorage`, решение прежнее), автообновление очереди
заданий (таймер должен умирать вместе с вкладкой — иначе фоновые запросы с телефона).

### 2.3 Телефон

**Брейкпоинты:** 900 (планшет/узкий десктоп), 620 (телефон-ландшафт), 430 (телефон).

**Шапка и вкладки.** Убрать магию `top:53px`. Хедер и полоса вкладок — один
`position:sticky` контейнер, тогда смещение считать не нужно вовсе. Чипы статистики на
узком — горизонтальный скролл-ряд (`overflow-x:auto`, `flex-wrap:nowrap`), без переноса.

**Контролы.** При `max-width:620`: `.row{flex-direction:column;align-items:stretch}`,
`label.f.grow{flex-basis:100%}`, `.panel{padding:12px 12px}`, кнопки во всю ширину,
минимальная площадь касания 44×44 (сейчас `.ghost.sm` меньше).

**Таблицы.** Два разных решения, потому что задачи разные:

- «Идеи», «Тезисы», «Источники», таблица попаданий циферблата — на узком превращаются в
  карточки: каждая строка — блок, подписи из `<th>` едут в `data-label` и печатаются
  псевдоэлементом. Колонок 6–7, скролл тут бесполезен.
- Служебные (очередь заданий, отказы арбитра) — оставить скролл, туда с телефона не смотрят.

**Циферблат.**

- Квадрат по ширине: `h = min(w, 0.7 * innerHeight)`, сейчас высота от окна независимо от
  ширины (`console.html:736`).
- Точки-листья при `w < 430` по умолчанию **выключены**, и статус это говорит словами
  («листья скрыты: экран 390 px»). Число `total` в статусе не меняется — оно про ответ,
  а не про то, что нарисовано.
- Радиусы меток и `gapPx` раскладки (`ringLayout`, `console.html:626`) масштабировать от
  ширины: на 390 px узел ~5 px, метка попадания ~5 px, кольцевой зазор ~12 px.
- Число идей по умолчанию на узком — 15, не 30 (в кольцо на 390 px больше не встаёт;
  причина та же, что уже описана в README).

**Тач вместо ховера.**

- `mousemove`/`mouseleave` (`console.html:942`, `:988`) заменить на `pointermove` +
  `pointerdown`, ветка по `event.pointerType`.
- Палец: тап по метке — показать карточку; тап по той же метке второй раз — открыть идею
  (для узла-идеи); тап мимо — скрыть. Никакого «наведения» и никакой зависимости от
  `:hover` в CSS для содержательных элементов.
- Радиус поиска ближайшей метки: 9 px мышью, 18 px пальцем (сейчас порог 9 —
  `console.html:951`).
- При `max-width:620` карточка не следует за пальцем, а прижимается к низу экрана на всю
  ширину (палец её иначе закрывает). Позиционирование в `console.html:985-986` уходит под
  медиа-условие.
- `@media (hover:none)` — снять `:hover`-эффекты, которые на тач-экране залипают
  (`table.tbl tr:hover`, `console.html:99`; `circle.ideanode:hover`).

**Чего не делать.** Не переходить на canvas ради телефона, пока не измерено: 3 040 SVG-кругов
на мобильном движке могут держаться; мерить `performance.now()` вокруг `drawDial` на
реальном телефоне и решать по числу, а не по ощущению. Не прятать колонки «по-умному»:
скрытая колонка — это число, которого нет на экране, а мы уже договорились, что число либо
верное, либо отсутствует явно.

---

## §3 Проверки

Браузерной логики `selfcheck` не гоняет — Python-проверка HTML-страницы видит только текст.
Поэтому проверки делятся на три уровня, и первый из них новый.

**Самопроверка страницы.** В `console.html` добавить блок, который выполняется только при
`?selftest=1` и печатает в консоль браузера одну строку `SELFTEST OK` или падает на
первом `assert`:

- `parseHash("#ideas/idea_ab12") → {tab:"ideas", arg:"idea_ab12"}`, и обратно
  `formatHash` даёт исходную строку;
- `parseHash("#dial?q=a%20b&k=3")` возвращает параметры декодированными;
- `parseHash("")` → вкладка по умолчанию, без `arg`;
- `VIEW_STATE` после двух `switchTo` туда-обратно содержит прежний объект (тождество, не копия);
- «ближайшая метка» на синтетическом наборе марок выбирает попадание, а не лист, когда они
  в одной точке (проверка веса слоёв, `console.html:948`).

**`lake/api/selfcheck.py`** — статически, тем же способом, что уже проверяет `node --check`
и отсутствие идентификаторов озера:

- страница содержит блок `selftest` (иначе проверка выше молча исчезнет);
- в файле нет `let openIdea = () => {}` — заглушка, из-за которой всё это началось;
- есть `<meta name="viewport">`;
- есть медиа-запросы под 620 и 430 (иначе «адаптив» уедет с первым же рефакторингом);
- нет `top:53px` и других зашитых высот шапки.

**Ручной чек-лист** (телефон, 390×844, реальный аппарат, не эмулятор):

1. Открыть `/ui#dial`, ввести гипотезу пальцем, разложить — статус, круг, таблица помещаются.
2. Тап по узлу — карточка снизу; повторный тап — открылась идея; кнопка «назад» вернула круг с прежним запросом.
3. Переключить вкладку и вернуться — гипотеза и ответ на месте, повторного запроса нет (видно по логу сервера).
4. Ссылку с `#dial?q=…&run=1` открыть в новой вкладке — разложилось само, те же числа.
5. Таблицы «Идеи» и «Тезисы» читаются без горизонтального скролла.

---

## §4 Операторские сценарии

Четыре критика прошли по интерфейсу 2026-08-02 разными взглядами. Ниже — то, что я
проверил в коде сам; находки, которые не подтвердились или стоят дороже пользы, названы в
конце раздела.

### 4.1 Тихие обманы — чинить первыми

| Что | Адрес | Почему это дороже, чем выглядит |
|---|---|---|
| Пагинатор двигает `offset` ДО того, как страница приехала: `state.offset += state.limit; reload()`, а `load()` при ошибке выходит, не откатив | `console.html:1124-1125`, выход `:1139` | Три неудачных «вперёд» по флапающей сети — и следующая удачная страница молча начнётся с 76-й строки. Пятьдесят идей пропущены, и ни одно число на экране об этом не скажет. Ровно тот класс тихой лжи, который проект запрещает в API |
| Подтверждение записи стирается собственным перечитыванием: `setStatus(patchStatus, …)` и сразу `show(ideaId)`, который первой строкой делает `detail.replaceChildren()` | `console.html:1200-1202`, `:1158` | «Записано» не отрисовывается никогда. Если PATCH прошёл, а перечитывание упало 503 — на экране только ошибка, и правку сделают второй раз |
| Недоступный сервер показан как отказ ключа: сетевую ошибку `boot` глотает `catch {}`, дальше `render()` открывает гейт с ПУСТЫМ сообщением | `console.html:1925-1931`, `:1915` | Оператор ищет новый ключ вместо того, чтобы посмотреть, жив ли процесс. Инфраструктурный отказ, надетый на авторизацию — то, чего API себе не позволяет |
| `202` и `200` в статусах вписаны страницей: `api()` возвращает только тело, код не отдаёт | `console.html:302`, литералы `:555`, `:1140`, `:1415`, `:1888` | Ручка, ответившая 201 или 204, всё равно покажет «200». По этой цифре нельзя диагностировать ничего, а выглядит она как факт от сервера |

**Решение.** `api()` возвращает `{status, body}` (или вешает `status` на результат);
`pager` присваивает новый `offset` только после успешного ответа; статус записи живёт в
хосте вне `detail`; `boot` различает `0` и `401` — при `0` рисует страницу со строкой «нет
связи с API · повторить», а не гейт, и `openGate("")` без текста не вызывается никогда.

### 4.2 Необратимое и дорогое — без подтверждения и без блокировки

- `disabled` не ставится ни на одной кнопке, кроме пагинатора (`console.html:1122`, `:1124`),
  хотя стиль `button.act[disabled]` уже написан (`:74`) — двойной тап шлёт второй `POST`.
  Цена: две строки в `retrieve.jsonl` (загрязнение того самого замера A/B), два прогона
  судьи 35B, `409` на фазе, слот которой занял сам оператор.
- `confirm()` в файле нет ни одного. При этом «Судить» с пустым полем — это суд по **всем**
  dirty-идеям (`console.html:1502-1505`), «Выгрузить в Obsidian» пересоздаёт папку целиком
  (`:1506-1513`), «Фаза 2» пишет в граф, а удаления в системе нет вовсе. Все три —
  `button.ghost`, то есть выглядят как «Обновить», и стоят с ней в одном ряду.
- Таймер автообновления очереди снимается только галкой (`console.html:1521-1524`);
  `render()` сносит DOM, а `setInterval` продолжает долбить сервер до конца сессии.

**Решение.** Обёртка `guard(fn)` рядом с `run()` (`console.html:350`): снимает и возвращает
`disabled` вокруг вызова, одна на все `button.act`. `confirm()` с числом перед тремя
дорогими действиями — число уже есть в `LAST_STATS` и в ответе `/vault/export`. Класс
`.ghost.danger` (правило `:79` написано и не используется) — чтобы вид отличался от
«Обновить». Таймеры регистрируются в состоянии вкладки и снимаются при уходе с неё — это
часть §2.2, иначе `VIEW_STATE` станет ещё и утечкой таймеров.

### 4.3 Долгая операция

`busy()` рисует статичную строку (`console.html:329`), `pool` не отдаёт прогресса (`:337`),
`loadGraph` тоже (`:597-618`). «Тяну идеи и рёбра каждой из них…» на пустом корне — это 261
запрос, и живой процесс неотличим от повисшего: `timeout` в `api()` — 120 с на запрос, а не
на операцию. Отмены нет; единственный выход — перезагрузка, которая стирает всё (§0.2).

**Решение.** `pool` принимает колбэк прогресса, `busy` дописывает «42/261»; рядом со
спиннером кнопка «Отмена», которая бампает `generation` и абортит текущий
`AbortController`. Вызов `/dial` получает свой `timeout: 15000` (`console.html:681`) — 285 КБ
за 170 мс локально, две минуты ожидания там не диагностика, а пустая сцена.

### 4.4 Дорога до идеи

Поиск по смыслу написан и работает — `resolveIdea()` (`console.html:1612`), — но пользуется
им только «Граф». Во вкладке «Идеи» поле принимает исключительно `idea_…` (`:1134`), поэтому
«поправить формулировку идеи про суррогатные модели» это: разложить гипотезу → найти узел →
скопировать id → уйти в «Идеи» → вставить → открыть.

**Решение.** То же поле принимает текст: не сошёлся `LOOKS_LIKE_ID` (`:1602`) — звать
`resolveIdea`, открывать лучшего кандидата, остальных положить кнопками, как уже сделано в
графе (`:1750-1760`).

Отдельно: очередь отказов арбитра (`console.html:1441-1455`) — тупик. Ни `thesis_id`, ни
ссылки на источник, ни списка кандидатов, ни действия; `limit=50` зашит, а про обрезку, в
отличие от соседней таблицы (`:1388`), не сказано. Минимум: `thesis_id`, ссылка на источник,
раскрытие кандидатов и та же оговорка про `limit`.

### 4.5 Что отклонено

- **Полный ARIA-паттерн вкладок** (roving `tabindex`, стрелки, `aria-controls`). Сейчас
  `role="tab"` заявлен наполовину (`console.html:1911`), а `nav` не `tablist` — заявленное
  поведение расходится с реальным, и это хуже отсутствия роли. Берём ленивый вариант:
  убрать `role="tab"`, поставить `aria-current="page"`, поправить селектор `:52`.
- **Пересборка шкалы кеглей в пять ступеней** целиком. Берём только нижний край (§5.3):
  поднять то, что мельче 12 px. Остальное — косметика, которая раздувает диф перед защитой.

---

## §5 Визуальная иерархия и честность картинки

Контрасты ниже я пересчитал сам (WCAG, sRGB), а не взял со слов.

### 5.1 Цвет, который врёт

| Что | Адрес | Измерено |
|---|---|---|
| Шкала циферблата инвертирована относительно тёмного фона: **ближайшие листья — самые невидимые** | `console.html:583` (`DIAL_RAMP`), `:745`, `:798` | тёмный конец `#1c5cab` при `fill-opacity .3` над `#1a1a19` даёт **1.26:1**; светлый конец `#cde2fb` там же — **2.35:1**. Дальний хвост озера вдвое громче ближнего края |
| Светлая тема не переопределяет статусные цвета | `console.html:14` объявляет, блок `:16-22` не перекрывает | на `#fcfcfb`: `--warning` **1.79:1**, `--serious` **2.57:1**, `--good` **3.27:1**. `.cos-mid{color:var(--warning)}` (`:129`) красит главный косинус в карточке — в светлой теме его не видно |
| `--muted` один и тот же в обеих темах | `console.html:11` и `:19` | на тёмном **5.13:1**, на светлом **3.50:1** — и этим цветом набраны все `th`, все подписи полей и легенда циферблата, 11 px капсом |

**Решение.** Рампа выбирается по теме: `TRUST_RAMP` (`console.html:1544`) — это тот же
массив в обратном порядке, новых констант не нужно. `leafAlpha` `0.3 → 0.45` (тёмный конец
поднимается до 1.46:1 — всё ещё фон, но различимый). В блок светлой темы добавить
`--muted:#6b6a65` (**5.28:1**), `--warning:#8a6100` (**5.40:1**), `--good:#0a7a0a`
(**5.38:1**), `--serious:#b4531f` (**4.87:1**); `--critical #d03b3b` — 4.68:1, оставить.

### 5.2 Подписи, которых не видно

`svg.append(gRings, gPts, gEdges, gIdeas, gHits, gLab)` (`console.html:928`) — кольца и их
числа идут ПЕРВЫМИ, то есть под всем остальным. Гало (`:163-164`) спасает от фона, но не от
узлов: на `scratchpad/topo6.png` от метки медианы осталось «0.5», остальное закрыто идеями.

**Решение.** Отдельная группа `gRingLab`, добавляется последней; `stroke-width` гало
`3.5px → 4.5px`.

### 5.3 Кегль против расстояния до зала

Самый мелкий шрифт проекта стоит на картинке, ради которой идёт доклад: `text.ringlab`
10 px (`console.html:165`), `text.hitlab` 11.5 px (`:166`), `text.hitrank` 9.5 px (`:169`).
`viewBox` идёт 1:1 к CSS-пикселям (`:736`, `:754`) — это ровно 10 px на экране.

**Решение.** `ringlab` 10 → 13 px, `hitlab` 11.5 → 14 px, `hitrank` 9.5 → 11 px и радиус
метки top-5 `6.5 → 8` (`:899`, иначе двузначная цифра не влезет). Проверить, что `gapPx`
раскладки (`:626`) ещё держит узлы врозь. Спицы к попаданиям — единственный честный носитель
смысла на картинке, а нарисованы слабее рёбер: `stroke-width 1 → 1.6`,
`stroke-opacity .45 → .7` (`:896-897`).

Главное число ответа тонет в предложении: «ближайший 0.653» набрано тем же 13 px, что и
слово «расставлены» (`console.html:700-701`). Вынести `cosine.max` в отдельный узел 21 px с
`tabular-nums`; медиану не увеличивать — она контекст, а не ответ.

Мелочи с той же ценой: `table.tbl th` выключены влево над числами, выключенными вправо
(`:94` против `:100`) — добавить `th.num`; id идей печатаются целиком 12.5 px моноширинным
(`:1009-1010`), из 20 символов пять одинаковы у всех — приглушить префикс `idea_` в `.dim`,
чтобы вес нёс различающий хвост (повторы идей в таблице станут видны глазом).

---

## §6 Клавиатура, скринридер, ввод

Проверено grep'ом: `aria-live` в файле нет ни разу, `role="dialog"` нет, `confirm` нет,
обработчик `keydown` ровно один — Enter в модалке ключа (`console.html:388`).

| Что | Адрес | Кто страдает |
|---|---|---|
| Модалка ключа — ловушка: ни `Esc`, ни «Отмена», ни `role="dialog"`; фокус уходит табом за спину окна | `console.html:206`, `:364-370` | Нажал «🔑 ключ» посмотреть — консоль заперта. `openGate` из `api()` при 401 (`:296`) ещё и крадёт фокус посреди набора текста |
| Статусы не объявляются: `setStatus` пишет в обычный `div` | `console.html:322`, шапка `:198` | Оператор со скринридером не услышит ни «считаю», ни «503 ОЗЕРО НЕДОСТУПНО». Чинится в одном месте: `aria-live` (`assertive` для `err`, иначе `polite`) на хосте — это лечит все восемь вкладок |
| Enter не отправляет ни одну форму: «формы» — это `div.row` | `console.html:565`, `:1106`, `:1281`, `:1838`, `:1876` | С клавиатуры и с телефона приходится табать до кнопки. `el("form", {onsubmit})` даёт неявную отправку бесплатно — но вторичным кнопкам в ряду обязателен `type:"button"` |
| Фокус уничтожается при каждом переключении вкладки и каждом листании | `console.html:1910` (`tabs.replaceChildren`), `:1124` | После каждого действия табать заново с начала страницы; своего `:focus-visible` у кнопок нет (`:66` покрывает только поля) |
| `.scroller` не фокусируется, а внутри нет ни одного фокусируемого элемента | `console.html:101`, содержимое `:1002`, `:1389`, `:1445` | Прокрутить таблицу с клавиатуры физически нельзя — строк ниже среза для такого оператора не существует |
| Узлы графа — клик по голому `<circle>` | `console.html:873-876`, `:1798-1805` | Без мыши граф недоступен целиком. У циферблата есть запасной путь (таблица со ссылками), у «Графа» — нет |
| Поля с id и путями без `autocapitalize`/`autocorrect`, поле ссылки — обычный `text` | `console.html:1134`, `:1406`, `:1249-1250`, `:1410`, `:1638`, `:1864` | iOS исправит `idea_8465d6` на `Idea_8465d6` → 404, причина которого на экране не видна |

**Решение.** Всё перечисленное — точечные правки, ни одна не требует переписывания:
`role="dialog" aria-modal` + `Esc` + «Отмена» + возврат фокуса; `aria-live` в `setStatus`;
`<form>` вместо `div.row` с `type:"button"` у вторичных; полоса вкладок строится один раз, а
`main` получает `tabindex="-1"` и фокус после монтирования; `.scroller` — `tabindex="0"` +
`role="region"` + `aria-label`; узлам-идеям — `tabindex`, `role="button"`, `aria-label`,
`Enter`/`Space` (листья в порядок табуляции НЕ пускать: 3040 остановок — это отказ в
обслуживании); полям — `autocapitalize:"off"`, `spellcheck:"false"`, полю ссылки
`type:"url"`, полям запроса `enterkeyhint:"search"`. Одно правило CSS:
`button:focus-visible{outline:2px solid var(--s1);outline-offset:2px}`.

---

## §7 Сцена: защита 3 августа

Восемь из десяти рисков — не баги, а несоответствие кегля важности: правда про циферблат
написана серым 10 px, красивая неправда нарисована во весь экран. Дешёвое лечение — §5.3.
Сверх него:

1. **«А если ввести чушь?»** Статус зелёный при любом ответе (`console.html:697-701`), а
   порог «в озере нечего найти ≈0.48 / есть ≈0.63» живёт в `title`-подсказке на другой
   вкладке (`:466-467`). Правка: красить статус по `data.cosine.max` — ниже 0.48 → `warn` и
   словами «это шум, а не находка». Новых чисел не выдумываем, порог уже записан.
2. **Ввод кириллицей.** Индекс английский (`lake/embed.py:13`), тезисы английские. Русская
   фраза даст правдоподобные 0.3–0.4 и заполненную таблицу — то есть тихую деградацию.
   Правка: `if (/[а-яё]/i.test(text))` — приписка в статусе «запрос кириллицей, индекс
   английский: косинусы это шум».
3. **Шкала перенормируется под каждый запрос** (`console.html:739-744`), поэтому два разных
   ответа выглядят одинаково плотными. Дописать в статус край круга; показывать ответы
   последовательно, никогда не рядом.
4. **Метка попадания стоит не на своём радиусе.** Если идея попадания попала в показанные,
   точка рисуется в координатах идеи, чей радиус — косинус её ЛУЧШЕГО листа
   (`console.html:892-895` против легенды `:781`). Внимательный зритель сверит таблицу с
   картинкой. Дешёвая правка на день: дописать это в ховер и в легенду. Полная — рисовать
   на своём радиусе — ломает замысел спицы, к защите не надо.
5. **Вкладка «Ингест и ops» — один промах мышью до записи в граф при зрителях.**
   Демо-режим: `?safe=1` не регистрирует вкладки, которые пишут, и говорит об этом чипом в
   шапке. Именно не «спрятать по умолчанию» — оператору они нужны; выключается явно и видно.
6. **Чистая загрузка = чёрный экран, потом модалка ключа на проекторе**
   (`console.html:1925-1933`): `render()` вызывается ПОСЛЕ пробы с таймаутом 10 с. Перенести
   `render()` до пробы — вкладки и объяснение появляются сразу.

Действия докладчика, кода не требуют: зум браузера 150 % ДО первой отрисовки (SVG строится
от `wrap.clientWidth`, `:736` — текст масштабируется вместе с кругом); первый показ — со
снятой галкой «точки-листья», чтобы зал прочитал кольца, а не кластеры; прогнать одну и ту
же фразу дважды и показать, что картинка совпадает пиксель в пиксель (`ringLayout` без ГСЧ,
`:830-833`); сказать вслух, что `/dial` ничего не пишет — до того, как спросят.

---

## §8 Порядок

Сначала то, что врёт или теряет данные, потом то, что мешает работать, потом косметика.

1. **Тихие обманы** (§4.1): откат `offset`, статус записи, `0` против `401` на старте, код
   ответа из `api()`. Каждый — единицы строк, каждый сегодня показывает неправду.
2. **Роутер и `openIdea`** (§2.1) — снимает дефект, который видно первым.
3. **`VIEW_STATE` и параметры в URL** (§2.2), вместе со снятием таймеров при уходе с вкладки
   (§4.2) — иначе состояние станет ещё и утечкой таймеров.
4. **Защита от повторного нажатия и `confirm` на дорогом** (§4.2) — до защиты, а не после:
   лишний прогон 35B стоит денег, а лишняя строка в `retrieve.jsonl` — замера.
5. **Тач вместо ховера** (§2.3) — без него телефон бесполезен, это регрессия.
6. **Кегль, z-порядок подписей, цвета светлой темы** (§5) — то, без чего доклад с проектора
   не читается.
7. **Клавиатура и скринридер** (§6): `aria-live`, `<form>`, фокус, модалка.
8. **Раскладка и таблицы** (§2.3, остальное), поиск по смыслу в «Идеях» (§4.4).
9. **Демо-режим `?safe=1`, пороги и предупреждения** (§7) — если остаётся время до защиты.
10. **Проверки** (§3) — вместе с каждым пунктом, не в конце.

---

## §9 Открытые вопросы владельцу

1. **Что показывать на телефоне по умолчанию** — циферблат или retrieve? Сейчас первая
   вкладка `retrieve` (`console.html:1900`), а тыкать с телефона будут гипотезу.
2. **Нужна ли ссылка «поделиться»** отдельной кнопкой, или хватит того, что адрес и так
   стал воспроизводимым.
3. **`run=1` в ссылке** — согласовать: это единственный способ, которым консоль ходит в
   сеть без нажатия. Для `/dial` он безвреден (чтение индекса, лог не пишется), для
   `/retrieve` — нет, там строка в `retrieve.jsonl`. Предлагаю разрешить `run=1` только
   на циферблате.
