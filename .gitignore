# Credentials — never committed (CLAUDE.md: ключи наружу не выносить)
Neo4j-*.txt
.env
.env.*
!.env.local.example
*.credentials
# Ноутбук школы содержит два ключа в открытом виде.
# Снять из игнора можно после переноса ключей в os.environ (10 §2: LAKE_KEY_9B / LAKE_KEY_35B).
LLM-серверы — подключение.ipynb

# Runtime data (10-implementation-spec.md §2)
data/

# Скриншоты демо-страницы — пересобираются из index.html за секунды (demo/shoot.py)
demo/shots/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# macOS
.DS_Store

# Промежуточные кадры manim (готовые ролики — tools/anim/out/)
tools/anim/media/
